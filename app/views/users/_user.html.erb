<li>
  <%= gravatar_for user, size: Settings.user.gravatar_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
  | <%= link_to t("users.delete"), user, method: :delete, data: {confirm: t("users.user.confirm_delete")} %>
  <% end %>
</li>
